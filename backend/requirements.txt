# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.18

# Database
sqlmodel==0.0.22
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
google-auth>=2.29.0

# Unit Conversion
pint>=0.24.1

# Task Queue (for Excel imports)
arq==0.26.0
redis>=4.2.0,<5

# Data Processing
openpyxl==3.1.2
pandas==2.2.0

# PDF Generation
reportlab>=4.1.0

# Validation & Settings
pydantic-settings==2.5.0
email-validator==2.1.0

# HTTP Client
httpx==0.27.0

# S3-compatible Storage (AWS S3, Cloudflare R2)
boto3>=1.34.0

# AI Services
anthropic>=0.35.0

# CLI
typer==0.12.0

# Testing
pytest==8.3.0
# 0.23.0 crashes during collection ("'Package' object has no attribute 'obj'")
# when tests/ is a package (tests/__init__.py present); 0.24+ fixes it.
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Rate Limiting
slowapi>=0.1.9

# Monitoring
sentry-sdk[fastapi]==2.19.0

# Billing
stripe==11.0.0

# Dev — keep in sync with the pinned versions in .github/workflows/ci.yml
black==26.3.1
ruff==0.6.9
aiosqlite==0.22.1
