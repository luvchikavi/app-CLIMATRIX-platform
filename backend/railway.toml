[build]
builder = "nixpacks"

[deploy]
startCommand = "python migrate.py && python -m alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
